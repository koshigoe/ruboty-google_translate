require "spec_helper"

RSpec.describe Ruboty::GoogleTranslate do
  it "has a version number" do
    expect(Ruboty::GoogleTranslate::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
